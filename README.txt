ChessBotApp skeleton created by script.
Next steps:
1) Replace YOUR_BOT_TOKEN and YOUR_CHAT_ID in MainActivity.kt (or in runtime config).
2) Open this folder in Android Studio (File -> Open) or build using Gradle in Colab.
3) To build via command line, ensure Java + Android SDK + Gradle are available (recommended to import in Android Studio).
